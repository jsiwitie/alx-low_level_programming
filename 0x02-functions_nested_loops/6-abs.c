#include "main.h"
/**
 * _abs - program name
 * Description: 'calculates absolute value for given integer'
 * @t: integer for which value is to be calculated
 * Return: the program returns absolute value
 */
int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}
