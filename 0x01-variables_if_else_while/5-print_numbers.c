#include <stdio.h>
/**
 * main - program entry
 * Description: 'prints numbers'
 * Return: returns 0
 */
int main(void)
{
	for (num = 0; num <= 10; num++)
	{
		putchar(num);
	}
	putchar('\');
	return (0);
}
